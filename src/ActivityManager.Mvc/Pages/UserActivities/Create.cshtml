@page
@model ActivityManager.Pages.UserActivities.CreateModel

@{
    ViewData["Title"] = "Create";

    var activities = ViewData["Activities"] as SelectList;
}

<h1>Create</h1>

<h4>UserActivity</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NewUserActivityInfo.ActivityId" class="control-label"></label>
                <select asp-for="NewUserActivityInfo.ActivityId" class="form-control" asp-items="activities"></select>
                <span asp-validation-for="NewUserActivityInfo.ActivityId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
